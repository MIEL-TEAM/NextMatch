# 🎨 OAuth Flow Diagrams - Visual Analysis

## 🔴 **Current Flow (BROKEN)**

### Register/Login with Google Flow

```
┌─────────────────────────────────────────┐
│  User on /register or /login page       │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  <SocialLogin /> component renders      │
│  • Google button shown                  │
│  • Facebook button shown                │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  User clicks "Sign in with Google"      │
│                                          │
│  onClick("google") called                │
│  ├─ signIn('google', {                  │
│  │    callbackUrl: "/members" ❌       │
│  └─ })                                   │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  Google OAuth (External)                │
│  • User authenticates                   │
│  • Google verifies email                │
│  • Returns to app                       │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  NextAuth signIn Callback               │
│  File: src/auth.ts (lines 41-117)      │
│                                          │
│  What happens:                           │
│  ✅ Update User:                        │
│     • emailVerified = new Date()        │
│     • oauthVerified = true              │
│     • provider = "google"               │
│     • trustScore += 40                  │
│                                          │
│  ❌ Does NOT:                           │
│     • Check if Member exists            │
│     • Create Member                     │
│     • Check profileComplete             │
│                                          │
│  Returns: true (allow sign-in)          │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  NextAuth Redirect                      │
│  • No custom redirect callback          │
│  • Uses callbackUrl from signIn()       │
│  • Redirects to: /members ❌           │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  User lands on /members page            │
│                                          │
│  ❌ PROBLEM:                            │
│  • User has NO Member record            │
│  • profileComplete: false               │
│  • User is INVISIBLE in list            │
│                                          │
│  ❌ /complete-profile page bypassed     │
│  ❌ completeSocialLoginProfile() never  │
│     called                               │
└─────────────────────────────────────────┘
```

---

## ✅ **Expected Flow (SHOULD WORK LIKE THIS)**

### Register/Login with Google - New User

```
┌─────────────────────────────────────────┐
│  User on /register or /login page       │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  User clicks "Sign in with Google"      │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  Google OAuth (External)                │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  NextAuth signIn Callback               │
│  • Create/Update User                   │
│  • Set emailVerified ✅                 │
│  • Set oauthVerified ✅                 │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  NextAuth redirect Callback ⚠️ NEEDED  │
│                                          │
│  Check: Does user have Member?           │
│  ├─ YES → Redirect to /members          │
│  └─ NO  → Redirect to /complete-profile │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  User lands on /complete-profile page   │
│                                          │
│  ✅ CompleteProfileForm renders         │
│  ✅ User fills dating profile:          │
│     • Gender                             │
│     • Date of Birth                      │
│     • City                               │
│     • Country                            │
│     • Description                        │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  User submits form                      │
│                                          │
│  completeSocialLoginProfile() called    │
│  File: authActions.ts (line 325)       │
│                                          │
│  What happens:                           │
│  ✅ Update User:                        │
│     • profileComplete = true            │
│     • emailVerified confirmed           │
│                                          │
│  ✅ Create Member via upsert:           │
│     • name                               │
│     • gender                             │
│     • dateOfBirth                        │
│     • city                               │
│     • country                            │
│     • description                        │
│                                          │
│  Returns: { status: "success" }         │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  CompleteProfileForm.onSubmit           │
│  • Calls signIn(provider)               │
│  • Refreshes session                    │
│  • Redirects to /members                │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  User lands on /members page            │
│                                          │
│  ✅ SUCCESS:                            │
│  • User has Member record               │
│  • profileComplete: true                │
│  • User is VISIBLE in list              │
│  • Can match with others                │
└─────────────────────────────────────────┘
```

---

### Register/Login with Google - Returning User

```
┌─────────────────────────────────────────┐
│  Returning user clicks                  │
│  "Sign in with Google"                  │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  Google OAuth (External)                │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  NextAuth signIn Callback               │
│  • Update User (last login, etc.)       │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  NextAuth redirect Callback             │
│                                          │
│  Check: Does user have Member?           │
│  ├─ YES ✅ → Redirect to /members       │
│  └─ NO  ❌ → Redirect to /complete-     │
│              profile (shouldn't happen)  │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  User lands on /members page            │
│                                          │
│  ✅ Already has Member record           │
│  ✅ profileComplete: true               │
│  ✅ Can see matches immediately         │
└─────────────────────────────────────────┘
```

---

## 🔍 **Gap Analysis - What's Missing**

### Current Implementation vs Expected

| Component                      | Current                                         | Expected                             | Status         |
| ------------------------------ | ----------------------------------------------- | ------------------------------------ | -------------- |
| **SocialLogin.tsx**            | Hardcoded callbackUrl: "/members"               | Dynamic or no callbackUrl            | ❌ WRONG       |
| **auth.ts signIn callback**    | Updates User only                               | Updates User (correct)               | ✅ OK          |
| **auth.ts redirect callback**  | Does NOT exist                                  | Check Member, redirect accordingly   | ❌ MISSING     |
| **middleware.ts**              | Redirects auth users from /register to /members | Should allow /complete-profile       | ⚠️ PARTIAL     |
| **completeSocialLoginProfile** | Function exists but never called                | Should be called for new OAuth users | ❌ UNREACHABLE |

---

## 🔧 **Fix Comparison - Three Options**

### Option 1: Change callbackUrl to /complete-profile

```
┌─────────────────────────────────────────┐
│  signIn('google', {                     │
│    callbackUrl: "/complete-profile"     │
│  })                                      │
└──────────────┬──────────────────────────┘
               │
               ▼
         All users go to
        /complete-profile
               │
               ├─ New user: Fills form ✅
               └─ Returning user: Unnecessary ❌
```

**Pros:**

- Simple one-line change
- Forces profile completion

**Cons:**

- Returning users see profile form again
- Bad UX for existing users

**Verdict:** ❌ NOT RECOMMENDED

---

### Option 2: Add authorized callback

```
┌─────────────────────────────────────────┐
│  NextAuth authorized callback           │
│  Checks on EVERY request                │
└──────────────┬──────────────────────────┘
               │
               ▼
    if (!profileComplete &&
        pathname !== "/complete-profile")
               │
               ▼
    Redirect to /complete-profile
```

**Pros:**

- Enforces profile completion app-wide
- Catches all edge cases

**Cons:**

- Runs on every request (performance)
- More complex logic

**Verdict:** ⚠️ GOOD BUT OVERKILL

---

### Option 3: Add redirect callback (RECOMMENDED)

```
┌─────────────────────────────────────────┐
│  signIn('google')  (no callbackUrl)     │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  NextAuth redirect callback             │
│  Called ONCE after OAuth                │
└──────────────┬──────────────────────────┘
               │
               ▼
     Check: profileComplete?
               │
       ┌───────┴───────┐
       │               │
      YES              NO
       │               │
       ▼               ▼
   /members    /complete-profile
```

**Pros:**

- Clean separation of concerns
- Only runs once per sign-in
- Handles both new and returning users
- Follows NextAuth best practices

**Cons:**

- Requires changes in 2 files

**Verdict:** ✅ RECOMMENDED

---

## 📊 **Data Flow - Before vs After Fix**

### Before Fix (Current - BROKEN)

```
User Table                Member Table
┌─────────────────┐      ┌─────────────────┐
│ id: "user123"   │      │ (empty)         │
│ email: "..."    │      │                 │
│ emailVerified:  │      │                 │
│   2025-01-15    │      │                 │
│ profileComplete:│      │                 │
│   false ❌      │      │                 │
│ oauthVerified:  │      │                 │
│   true          │      │                 │
└─────────────────┘      └─────────────────┘
          │
          │ No Member! ❌
          ▼
    INVISIBLE in
    members list
```

---

### After Fix (Expected - WORKING)

```
User Table                Member Table
┌─────────────────┐      ┌─────────────────┐
│ id: "user123"   │◄─────┤ userId:"user123"│
│ email: "..."    │      │ name: "John"    │
│ emailVerified:  │      │ gender: "male"  │
│   2025-01-15    │      │ city: "Tel Aviv"│
│ profileComplete:│      │ country:"Israel"│
│   true ✅       │      │ dateOfBirth: ...│
│ oauthVerified:  │      │ description:"..." │
│   true          │      └─────────────────┘
└─────────────────┘
          │
          │ Has Member! ✅
          ▼
     VISIBLE in
    members list ✅
```

---

## 🎯 **Summary**

### The Problem (Visual)

```
OAuth User Flow:
Register → Google Auth → User Updated → /members → ❌ NO MEMBER

Expected Flow:
Register → Google Auth → User Updated → /complete-profile →
Member Created → /members → ✅ HAS MEMBER
```

### The Gap

```
Missing Link:
┌──────────────┐          ┌──────────────┐
│ Google Auth  │    ???   │ /complete-   │
│   Success    ├─────────►│  profile     │
└──────────────┘          └──────────────┘
     │                           │
     │                           │
     └─────────────┬─────────────┘
                   ▼
            Currently skips
           this entire step!
```

### The Fix

```
Add redirect callback:
┌──────────────┐          ┌──────────────┐
│ Google Auth  │  CHECK   │ Has Member?  │
│   Success    ├─────────►│              │
└──────────────┘          └───┬──────┬───┘
                              │      │
                             YES    NO
                              │      │
                              ▼      ▼
                         /members  /complete-
                                   profile
```

---

**Next: Implement the fix with Option 3 (redirect callback) ✅**
