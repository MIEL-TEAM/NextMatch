/**
 * Deployment fingerprint for production incident investigation.
 * Update this string with each investigation build so Vercel logs
 * can confirm which deployment is serving each request.
 *
 * INVESTIGATION — remove once quota behavior is confirmed stable.
 */
export const BUILD_FINGERPRINT = "quota-investigation-v1";
